# Codoxear Rust Cutover Endpoint Inventory

Source of truth: `codoxear/server.py` at OpenSpec change `rust-backend-cutover` baseline commit `ef8b81d`. This inventory is intentionally mechanical: each row includes the exact Python route-match expression and handler line range so reviewers can re-derive it with `grep`/AST inspection.

Ref comparison target: `san-tian/codoxear@san-tian-dev` (`backend-rs/src/routes.rs`, fetched while preparing this document). `present` means the route exists in the Rust ref with an equivalent handler. `missing in ref / huapeixuan-only` means the current target has Python behavior that must be ported into the future Rust phase even though it is not in the reference Rust router.

## Inventory

| Method | Path pattern | Exact Python match expression | Query params consumed | JSON request fields parsed | Response shape / top-level keys | Auth | Handler range | Ref comparison |
|---|---|---|---|---|---|---|---|---|
| GET | `/api/me` | `path == "/api/me"` | none | n/a | JSON `ok` | `_require_auth` | `codoxear/server.py:8775-8780` | present: `/api/v1/me`, `/api/me` (`me`) |
| GET | `/api/settings/voice` | `path == "/api/settings/voice"` | none | n/a | JSON `ok` + voice settings/status keys from `settings_snapshot()` | `_require_auth` | `codoxear/server.py:8782-8789` | present: `/api/v1/settings/voice`, `/api/settings/voice` (`settings_voice`) |
| GET | `/api/notifications/subscription` | `path == "/api/notifications/subscription"` | none | n/a | JSON `ok` + subscription snapshot keys | `_require_auth` | `codoxear/server.py:8791-8800` | present: `/api/v1/notifications/subscription`, `/api/notifications/subscription` (`notification_subscriptions`) |
| GET | `/api/notifications/message` | `path == "/api/notifications/message"` | `message_id` | n/a | JSON success `ok` + notification state keys; errors `error` | `_require_auth` | `codoxear/server.py:8802-8816` | present: `/api/v1/notifications/message`, `/api/notifications/message` (`notification_message`) |
| GET | `/api/notifications/feed` | `path == "/api/notifications/feed"` | `since` | n/a | JSON `ok`, `items`; errors `error` | `_require_auth` | `codoxear/server.py:8818-8831` | present: `/api/v1/notifications/feed`, `/api/notifications/feed` (`notification_feed`) |
| GET | `/api/audio/live.m3u8` | `path == "/api/audio/live.m3u8"` | none | n/a | HLS bytes (`Content-Type: application/vnd.apple.mpegurl`) | `_require_auth` | `codoxear/server.py:8833-8846` | present: `/api/v1/audio/live.m3u8`, `/api/audio/live.m3u8` (`audio_playlist`) |
| GET | `/api/audio/segments/{segment}` | `path.startswith("/api/audio/segments/")` | none | n/a | MPEG-TS segment bytes (`Content-Type: video/mp2t`) or HTTP 404 | `_require_auth` | `codoxear/server.py:8848-8867` | present: `/api/v1/audio/segments/*path`, `/api/audio/segments/*path` (`audio_segment`) |
| GET | `/api/sessions/bootstrap` | `path == "/api/sessions/bootstrap"` | none | n/a | JSON `recent_cwds`, `cwd_groups`, `new_session_defaults`, `tmux_available` | `_require_auth` | `codoxear/server.py:8869-8883` | partial/missing exact path in ref: ref has `/api/v1/bootstrap` and `/api/bootstrap`, not `/api/sessions/bootstrap`; Phase 1 must preserve this huapeixuan path/alias |
| GET | `/api/sessions` | `path == "/api/sessions"` | `view`, `group_key`, `offset`, `limit`, `group_offset`, `group_limit` | n/a | JSON session list payload (`view`, grouped/recent session data, pagination, `recent_cwds`, `cwd_groups`, `new_session_defaults`, `tmux_available`, `app_version` depending view); errors `error`, `view` | `_require_auth` | `codoxear/server.py:8885-8958` | present: `/api/v1/sessions`, `/api/sessions` (`sessions`), but target pagination/group fields need parity check |
| GET | `/api/session_resume_candidates` | `path == "/api/session_resume_candidates"` | `cwd`, `backend`, `agent_backend` | n/a | JSON `ok`, cwd description keys, `sessions`; errors `error`, `field` | `_require_auth` | `codoxear/server.py:8960-9009` | present: `/api/v1/session_resume_candidates`, `/api/session_resume_candidates` (`session_resume_candidates`) |
| GET | `/api/metrics` | `path == "/api/metrics"` | none | n/a | JSON `metrics` | `_require_auth` | `codoxear/server.py:9011-9016` | missing in ref / huapeixuan-only; Phase 2 port |
| GET | `/api/sessions/{session_id}/live` | `path.startswith("/api/sessions/") and path.endswith("/live")` | `offset`, `live_offset`, `requests_version` | n/a | JSON live/message payload plus errors `error` | `_require_auth` | `codoxear/server.py:9018-9061` | ref equivalent path is `/api/v1/sessions/:session_id/messages/live` and `/api/sessions/:session_id/messages/live`; legacy `/live` is huapeixuan-only alias; Phase 2 port |
| GET | `/api/sessions/{session_id}/workspace` | `path.startswith("/api/sessions/") and path.endswith("/workspace")` | none | n/a | JSON workspace descriptor or `error` | `_require_auth` | `codoxear/server.py:9063-9081` | missing in ref / huapeixuan-only; Phase 2 port |
| GET | `/api/sessions/{session_id}/details` | `path.startswith("/api/sessions/") and path.endswith("/details")` | none | n/a | JSON session details or `error` | `_require_auth` | `codoxear/server.py:9083-9098` | missing in ref / huapeixuan-only; Phase 2 port |
| GET | `/api/sessions/{session_id}/diagnostics` | `path.startswith("/api/sessions/") and path.endswith("/diagnostics")` | none | n/a | JSON diagnostics keys including `session_id`, `thread_id`, `agent_backend`/`backend`, `transport`, `cwd`, `log_path`, `session_file_path`, `codex_pid`, `broker_pid`, `owned`, `busy`, `start_ts`, `updated_ts`; errors `error` | `_require_auth` | `codoxear/server.py:9100-9231` | present: `/api/v1/sessions/:session_id/diagnostics`, `/api/sessions/:session_id/diagnostics` (`diagnostics`) |
| GET | `/api/sessions/{session_id}/queue` | `path.startswith("/api/sessions/") and path.endswith("/queue")` | none | n/a | JSON `ok`, `queue`; errors `error` | `_require_auth` | `codoxear/server.py:9233-9251` | present: `/api/v1/sessions/:session_id/queue`, `/api/sessions/:session_id/queue` (`queue`) |
| GET | `/api/sessions/{session_id}/ui_state` | `path.startswith("/api/sessions/") and path.endswith("/ui_state")` | none | n/a | JSON UI state (`priority_offset`, `snooze_until`, `dependency_session_id`) or `error` | `_require_auth` | `codoxear/server.py:9253-9271` | missing in ref / huapeixuan-only; Phase 2 port |
| GET | `/api/sessions/{session_id}/commands` | `session_id = _match_session_route(path, "commands")` then `session_id is not None` | none | n/a | JSON Pi command snapshot or `error` | `_require_auth` | `codoxear/server.py:9273-9287` | missing in ref / huapeixuan-only (Pi command UI); Phase 2 port |
| GET | `/api/sessions/{session_id}/file/read` | `path.startswith("/api/sessions/") and path.endswith("/file/read")` | `path` | n/a | JSON file view: `ok`, `kind`, `path`, `rel`, `size`, optional `text`, `editable`, `version`, `content_type`, `image_url`, `pdf_url`, `download_only`, `reason`, `viewer_max_bytes`; errors `error` | `_require_auth` | `codoxear/server.py:9289-9390` | present: `/api/v1/sessions/:session_id/file/read`, `/api/sessions/:session_id/file/read` (`file_read`) |
| GET | `/api/sessions/{session_id}/file/search` | `path.startswith("/api/sessions/") and path.endswith("/file/search")` | `q`, `limit` | n/a | JSON `ok`, `query`, `cwd`, `mode`, `matches`, `scanned`, `truncated`; errors `error` | `_require_auth` | `codoxear/server.py:9392-9449` | present: `/api/v1/sessions/:session_id/file/search`, `/api/sessions/:session_id/file/search` (`file_search`) |
| GET | `/api/sessions/{session_id}/file/list` | `path.startswith("/api/sessions/") and path.endswith("/file/list")` | `path` | n/a | JSON `ok`, `cwd`, `path`, `entries`; errors `error` | `_require_auth` | `codoxear/server.py:9451-9491` | missing in ref / huapeixuan-only; Phase 2 port |
| GET | `/api/sessions/{session_id}/file/blob` | `path.startswith("/api/sessions/") and path.endswith("/file/blob")` | `path` | n/a | Inline file bytes for image/pdf; JSON errors `error` | `_require_auth` | `codoxear/server.py:9493-9542` | present: `/api/v1/sessions/:session_id/file/blob`, `/api/sessions/:session_id/file/blob` (`file_blob`) |
| GET | `/api/files/blob` | `path == "/api/files/blob"` | `path` | n/a | Inline file bytes for image/pdf; JSON errors `error` | `_require_auth` | `codoxear/server.py:9544-9579` | missing in ref / huapeixuan-only global file viewer; Phase 2 port |
| GET | `/api/sessions/{session_id}/file/download` | `path.startswith("/api/sessions/") and path.endswith("/file/download")` | `path` | n/a | Attachment bytes with `Content-Disposition: attachment`; JSON errors `error` | `_require_auth` | `codoxear/server.py:9581-9625` | missing in ref / huapeixuan-only; Phase 2 port |
| GET | `/api/sessions/{session_id}/git/changed_files` | `path.startswith("/api/sessions/") and path.endswith("/git/changed_files")` | none | n/a | JSON `ok`, `cwd`, `files`, `entries`, `staged`, `unstaged`; errors `error` | `_require_auth` | `codoxear/server.py:9627-9739` | present: `/api/v1/sessions/:session_id/git/changed_files`, `/api/sessions/:session_id/git/changed_files` (`changed_files`) |
| GET | `/api/sessions/{session_id}/repo` | `path.startswith("/api/sessions/") and path.endswith("/repo")` | `refresh` | n/a | JSON repo/PR summary payload or `error` | `_require_auth` | `codoxear/server.py:9741-9765` | missing in ref / huapeixuan-only PR badge (`git_context`); Phase 2 port |
| GET | `/api/sessions/{session_id}/git/diff` | `path.startswith("/api/sessions/") and path.endswith("/git/diff")` | `path`, `staged` | n/a | JSON `ok`, `cwd`, `path`, `staged`, `diff`; errors `error` | `_require_auth` | `codoxear/server.py:9767-9823` | present: `/api/v1/sessions/:session_id/git/diff`, `/api/sessions/:session_id/git/diff` (`git_diff`) |
| GET | `/api/sessions/{session_id}/git/file_versions` | `path.startswith("/api/sessions/") and path.endswith("/git/file_versions")` | `path` | n/a | JSON `ok`, `cwd`, `path`, `abs_path`, `base_exists`, `base_text`, `current_exists`, `current_text`, `current_size`; errors `error` | `_require_auth` | `codoxear/server.py:9825-9899` | present: `/api/v1/sessions/:session_id/git/file_versions`, `/api/sessions/:session_id/git/file_versions` (`git_file_versions`) |
| GET | `/api/sessions/{session_id}/messages` | `path.startswith("/api/sessions/") and path.endswith("/messages")` | `offset`, `limit`, `before`, `init` | n/a (`diag` is an internal parsed log key, not request JSON) | JSON message history/index payload plus errors `error` | `_require_auth` | `codoxear/server.py:9901-9966` | ref equivalent paths are `/messages/tail`, `/messages/history`, `/messages/live`; legacy `/messages` shape is huapeixuan-only; Phase 2 port |
| GET | `/api/sessions/{session_id}/tail` | `path.startswith("/api/sessions/") and path.endswith("/tail")` | none | n/a | JSON `tail` or `error` | `_require_auth` | `codoxear/server.py:9968-9980` | ref equivalent is `/messages/tail`; legacy `/tail` is huapeixuan-only alias; Phase 2 port |
| GET | `/api/sessions/{session_id}/takeover` | `path.startswith("/api/sessions/") and path.endswith("/takeover")` | none | n/a | JSON takeover descriptor or `error` | `_require_auth` | `codoxear/server.py:9982-9995` | missing in ref / huapeixuan-only terminal takeover; Phase 2 port |
| GET | `/api/sessions/{session_id}/harness` | `path.startswith("/api/sessions/") and path.endswith("/harness")` | none | n/a | JSON `ok` + harness config; errors `error` | `_require_auth` | `codoxear/server.py:9997-10012` | present: `/api/v1/sessions/:session_id/harness`, `/api/sessions/:session_id/harness` (`harness`) |
| POST | `/api/cwd_groups/edit` | `path == "/api/cwd_groups/edit"` | none | `cwd`, `label`, `collapsed`, `hidden`, `hidden_after_live_start_ts` | JSON `ok`, `cwd`, group entry keys; errors `error` | `_require_auth` | `codoxear/server.py:10040-10065` | missing in ref / huapeixuan-only directory grouping; Phase 3 port |
| POST | `/api/login` | `path == "/api/login"` | none | `password` | JSON `ok`; errors `error`; sets `codoxear_auth` cookie | public (no `_require_auth`) | `codoxear/server.py:10067-10084` | present: `/api/v1/login`, `/api/login` (`login`) |
| POST | `/api/logout` | `path == "/api/logout"` | none | body ignored | JSON `ok`; clears cookie | `_require_auth` | `codoxear/server.py:10086-10098` | present: `/api/v1/logout`, `/api/logout` (`logout`) |
| POST | `/api/settings/voice` | `path == "/api/settings/voice"` | none | voice settings object (`tts_enabled_for_narration`, model/provider/base-url/voice options accepted by `VoicePushCoordinator.set_settings`) | JSON `ok` + cleaned settings; errors `error` | `_require_auth` | `codoxear/server.py:10100-10117` | present: `/api/v1/settings/voice`, `/api/settings/voice` (`settings_voice_save`) |
| POST | `/api/sessions/{session_id}/takeover/open` | `path.startswith("/api/sessions/") and path.endswith("/takeover/open")` | none | body ignored | JSON takeover open result/descriptor or `error` | `_require_auth` | `codoxear/server.py:10119-10135` | missing in ref / huapeixuan-only terminal takeover; Phase 3 port |
| POST | `/api/notifications/subscription` | `path == "/api/notifications/subscription"` | none | `subscription`, `user_agent`, `device_label`, `device_class` | JSON `ok` + subscription record/snapshot; errors `error` | `_require_auth` | `codoxear/server.py:10137-10159` | present: `/api/v1/notifications/subscription`, `/api/notifications/subscription` (`notification_subscriptions_upsert`) |
| POST | `/api/notifications/subscription/toggle` | `path == "/api/notifications/subscription/toggle"` | none | `endpoint`, `enabled` | JSON `ok` + subscription record/snapshot; errors `error` | `_require_auth` | `codoxear/server.py:10161-10191` | present: `/api/v1/notifications/subscription/toggle`, `/api/notifications/subscription/toggle` (`notification_subscriptions_toggle`) |
| POST | `/api/notifications/test_push` | `path == "/api/notifications/test_push"` | none | body ignored | JSON `ok` + test push payload; errors `error` | `_require_auth` | `codoxear/server.py:10193-10205` | missing in ref / huapeixuan-only debug endpoint; Phase 3/5 port with voice push |
| POST | `/api/audio/listener` | `path == "/api/audio/listener"` | none | `client_id`, `enabled` | JSON `ok` + listener heartbeat payload; errors `error` | `_require_auth` | `codoxear/server.py:10207-10230` | present: `/api/v1/audio/listener`, `/api/audio/listener` (`audio_listener`) |
| POST | `/api/audio/test_announcement` | `path == "/api/audio/test_announcement"` | none | body ignored | JSON `ok` + queued test announcement payload; errors `error` | `_require_auth` | `codoxear/server.py:10232-10244` | missing in ref / huapeixuan-only debug endpoint; Phase 3/5 port with voice push |
| POST | `/api/sessions` | `path == "/api/sessions"` | none | `cwd`, `backend`, `agent_backend`, `args`, `resume_session_id`, `worktree_branch`, `model_provider`, `preferred_auth_method`, `model`, `reasoning_effort`, `service_tier`, `create_in_tmux` | JSON `ok` + spawn result; errors `error`, optional `field` | `_require_auth` | `codoxear/server.py:10246-10287` | present: `/api/v1/sessions`, `/api/sessions` (`session_create`), but target-specific fields such as `agent_backend`, Pi defaults, and worktree/tmux handling need parity check |
| POST | `/api/files/read` | `path == "/api/files/read"` | none | `path`, `session_id` | JSON file view: `ok`, `kind`, `path`, `size`, optional `text`, `editable`, `version`, `content_type`, `image_url`, `pdf_url`, `download_only`, `reason`, `viewer_max_bytes`; errors `error` | `_require_auth` | `codoxear/server.py:10289-10384` | missing in ref / huapeixuan-only global file viewer; Phase 3 port |
| POST | `/api/files/inspect` | `path == "/api/files/inspect"` | none | `path`, `session_id` | JSON `ok`, `path`, `kind`, `content_type`, `size`, `reason`, `viewer_max_bytes`; errors `error` | `_require_auth` | `codoxear/server.py:10386-10434` | missing in ref / huapeixuan-only global file viewer; Phase 3 port |
| POST | `/api/files/blob` | `path == "/api/files/blob"` | `path` | body ignored | Inline file bytes for image/pdf; JSON errors `error` | `_require_auth` | `codoxear/server.py:10436-10471` | missing in ref / huapeixuan-only global file viewer; Phase 3 port |
| POST | `/api/sessions/{session_id}/file/write` | `session_id = _match_session_route(path, "file", "write")` then `session_id is not None` | none | `path`, `text`, `create`, `version` | JSON `ok`, `path`, `rel`, `size`, `version`, `editable`; conflict errors include `error`, `conflict`, `path`, `version` | `_require_auth` | `codoxear/server.py:10473-10600` | present: `/api/v1/sessions/:session_id/file/write`, `/api/sessions/:session_id/file/write` (`file_write`) |
| POST | `/api/sessions/{session_id}/delete` | `session_id = _match_session_route(path, "delete")` then `session_id is not None` | none | body ignored | JSON `ok`; errors `error` | `_require_auth` | `codoxear/server.py:10602-10613` | present: `/api/v1/sessions/:session_id/delete`, `/api/sessions/:session_id/delete` (`session_delete`) |
| POST | `/api/sessions/{session_id}/edit` | `path.startswith("/api/sessions/") and path.endswith("/edit")` | none | `name`, `priority_offset`, `snooze_until`, `dependency_session_id` | JSON `ok`, `alias`, `priority_offset`, `snooze_until`, `dependency_session_id`; errors `error` | `_require_auth` | `codoxear/server.py:10615-10647` | present route (`session_edit`) but huapeixuan sidebar priority/dependency fields need parity; Phase 3 port |
| POST | `/api/sessions/{session_id}/rename` | `path.startswith("/api/sessions/") and path.endswith("/rename")` | none | `name` | JSON `ok`, `alias`; errors `error` | `_require_auth` | `codoxear/server.py:10649-10672` | present: `/api/v1/sessions/:session_id/rename`, `/api/sessions/:session_id/rename` (`session_rename`) |
| POST | `/api/sessions/{session_id}/send` | `path.startswith("/api/sessions/") and path.endswith("/send")` | none | `text`, `images` | Broker send response or errors `error`; Pi image composer support included | `_require_auth` | `codoxear/server.py:10674-10710` | present route (`session_send`) but `images` is huapeixuan-only composer delta; Phase 3 port |
| POST | `/api/sessions/{session_id}/ui_response` | `path.startswith("/api/sessions/") and path.endswith("/ui_response")` | none | arbitrary UI response object passed through to broker | JSON `ok`; errors `error` | `_require_auth` | `codoxear/server.py:10712-10734` | missing in ref / huapeixuan-only Pi AskUser UI; Phase 3 port |
| POST | `/api/sessions/{session_id}/enqueue` | `path.startswith("/api/sessions/") and path.endswith("/enqueue")` | none | `text`, `images` | Queue/enqueue response or errors `error`; Pi image composer support included | `_require_auth` | `codoxear/server.py:10736-10772` | present route (`session_enqueue`) but `images` is huapeixuan-only composer delta; Phase 3 port |
| POST | `/api/sessions/{session_id}/heartbeat` | `path.startswith("/api/sessions/") and path.endswith("/heartbeat")` | none | body ignored | JSON `ok` + heartbeat payload; errors `error` | `_require_auth` | `codoxear/server.py:10774-10790` | missing in ref / huapeixuan-only; Phase 3 port |
| POST | `/api/sessions/{session_id}/queue/delete` | `path.startswith("/api/sessions/") and path.endswith("/queue/delete")` | none | `index` | Queue mutation response or errors `error` | `_require_auth` | `codoxear/server.py:10792-10818` | present: `/api/v1/sessions/:session_id/queue/delete`, `/api/sessions/:session_id/queue/delete` (`queue_delete`) |
| POST | `/api/sessions/{session_id}/queue/update` | `path.startswith("/api/sessions/") and path.endswith("/queue/update")` | none | `index`, `text` | Queue mutation response or errors `error` | `_require_auth` | `codoxear/server.py:10820-10850` | present: `/api/v1/sessions/:session_id/queue/update`, `/api/sessions/:session_id/queue/update` (`queue_update`) |
| POST | `/api/sessions/{session_id}/harness` | `path.startswith("/api/sessions/") and path.endswith("/harness")` | none | `enabled`, `request`, `cooldown_minutes`, `remaining_injections`; rejects legacy `text` | JSON `ok` + harness config; errors `error` | `_require_auth` | `codoxear/server.py:10852-10919` | present: `/api/v1/sessions/:session_id/harness`, `/api/sessions/:session_id/harness` (`session_harness`) |
| POST | `/api/sessions/{session_id}/interrupt` | `path.startswith("/api/sessions/") and path.endswith("/interrupt")` | none | body ignored | JSON `ok`, `broker`; errors `error` | `_require_auth` | `codoxear/server.py:10921-10939` | present: `/api/v1/sessions/:session_id/interrupt`, `/api/sessions/:session_id/interrupt` (`session_interrupt`) |
| POST | `/api/sessions/{session_id}/inject_file` | `path.startswith("/api/sessions/") and (path.endswith("/inject_file") or path.endswith("/inject_image"))` | none | `data_b64`, `filename`, `attachment_index` | JSON `ok`, `path`, `inject_text`, `broker`; Pi conflict includes `error`, `backend`, `operation`; errors `error` | `_require_auth` | `codoxear/server.py:10941-11035` | present route (`session_inject_attachment`), but target staging limits/path semantics must be checked; Phase 3 port |
| POST | `/api/sessions/{session_id}/inject_image` | `path.startswith("/api/sessions/") and (path.endswith("/inject_file") or path.endswith("/inject_image"))` | none | `data_b64`, `filename`, `attachment_index` | Same as `/inject_file` | `_require_auth` | `codoxear/server.py:10941-11035` | present route (`session_inject_attachment`); Phase 3 port |
| POST | `/api/hooks/notify` | `path == "/api/hooks/notify"` | none | body ignored | JSON `ignored` | public (no `_require_auth`) | `codoxear/server.py:11037-11041` | missing in ref / huapeixuan-only optional hook endpoint; Phase 3 port or explicitly preserve as no-op |

## Ref-only routes not present in huapeixuan Python baseline

These are in `san-tian-dev:backend-rs/src/routes.rs` but do not exist in the current target `server.py`; they are not Phase 0 implementation work, but future phase specs should decide whether to add, alias, or omit them:

- `GET /api/v1/health` and public `/api/health` — required by OpenSpec Phase 1 skeleton.
- `GET /api/v1/bootstrap` and `/api/bootstrap` — ref bootstrap path; target currently uses `/api/sessions/bootstrap`.
- `GET /api/v1/cwd_suggestions` / `/api/cwd_suggestions`.
- `GET|POST /api/v1/settings/codex_config` / `/api/settings/codex_config`.
- `POST /api/v1/settings/restart_service` / `/api/settings/restart_service`.
- `POST /api/v1/sessions/:session_id/queue/move` / `/api/sessions/:session_id/queue/move`.
- `GET /api/v1/sessions/:session_id/messages/{tail,history,live}` / `/api/sessions/:session_id/messages/{tail,history,live}` — ref split of target's legacy `/messages`, `/tail`, `/live`.
- `POST /api/v1/messages/send` / `/api/messages/send`.
- `GET /api/v1/events/stream` / `/api/events/stream`.

## Huapeixuan-only deltas

The following target behaviors are absent from the Rust reference router or only partially represented there. They must be explicitly carried into the relevant phase changes rather than silently dropped during Rust cutover.

### Must be ported in Phase 1

- `/api/sessions/bootstrap` legacy path. OpenSpec already calls out `/api/v1/sessions/bootstrap`; ref uses `/api/v1/bootstrap`. The target frontend currently calls `/api/sessions/bootstrap`, so Phase 1 must serve this path as well as any new canonical alias.

### Must be ported in Phase 2 (GET/read-only parity)

- PR badge / repo summary: `GET /api/sessions/{id}/repo` backed by `codoxear.git_context`.
- Session UI/detail helpers: `GET /workspace`, `/details`, `/ui_state`, `/commands`, `/takeover`.
- Legacy message aliases: `GET /messages`, `/tail`, `/live` (ref has split `/messages/{tail,history,live}` but frontend compatibility requires the old aliases).
- Global/session file helpers missing in ref: `GET /api/files/blob`, `GET /file/list`, `GET /file/download`.
- Metrics endpoint: `GET /api/metrics`.

### Must be ported in Phase 3 (POST/write parity)

- Directory grouping state: `POST /api/cwd_groups/edit` and `cwd_groups.json`.
- Global file viewer POST endpoints: `/api/files/read`, `/api/files/inspect`, `/api/files/blob`.
- Terminal takeover open: `POST /api/sessions/{id}/takeover/open`.
- Session heartbeat: `POST /api/sessions/{id}/heartbeat`.
- Pi AskUser/UI response bridge: `POST /api/sessions/{id}/ui_response`.
- Optional hooks no-op: `POST /api/hooks/notify` currently always returns `{"ignored": true}` without auth; preserve this unless a future spec explicitly removes it.
- Sidebar edit fields on `/edit`: `priority_offset`, `snooze_until`, `dependency_session_id`, persisted in `session_sidebar.json`.
- Composer image input on `/send` and `/enqueue`: JSON `images` is normalized for Pi sessions and must round-trip through Rust.

### Must be ported in Phase 5 (voice/debug parity)

- Voice debug/test endpoints absent from ref: `POST /api/notifications/test_push` and `POST /api/audio/test_announcement`.
- The target voice files are named `push_subscriptions.json` and `voice_delivery_ledger.json`; do not accidentally migrate to ref/spec draft names without compatibility aliases.

## Spot-check footer

Spot-check performed by `coding-agent` on 2026-05-09 against `codoxear/server.py` at commit `ef8b81d`:

1. `GET /api/sessions/bootstrap` row matches `path == "/api/sessions/bootstrap"` at `codoxear/server.py:8869-8883` and returns `recent_cwds`, `cwd_groups`, `new_session_defaults`, `tmux_available`.
2. `GET /api/sessions/{session_id}/repo` row matches `path.startswith("/api/sessions/") and path.endswith("/repo")` at `codoxear/server.py:9741-9765` and consumes `refresh`.
3. `POST /api/cwd_groups/edit` row matches `path == "/api/cwd_groups/edit"` at `codoxear/server.py:10040-10065` and parses `cwd`, `label`, `collapsed`, `hidden`, `hidden_after_live_start_ts`.
4. `POST /api/sessions/{session_id}/file/write` row matches `_match_session_route(path, "file", "write")` at `codoxear/server.py:10473-10600` and parses `path`, `text`, `create`, `version`.
5. `POST /api/hooks/notify` row matches `path == "/api/hooks/notify"` at `codoxear/server.py:11037-11041`, requires no auth, and returns `{"ignored": true}`.

Reviewer sign-off: pending independent review.
