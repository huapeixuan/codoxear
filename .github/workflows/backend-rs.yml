name: backend-rs

on:
  push:
    paths:
      - "backend-rs/**"
      - "tests/contract/**"
      - "web/**"
      - "pyproject.toml"
      - ".github/workflows/backend-rs.yml"
  pull_request:
    paths:
      - "backend-rs/**"
      - "tests/contract/**"
      - "web/**"
      - "pyproject.toml"
      - ".github/workflows/backend-rs.yml"

jobs:
  backend-rs:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: web/package-lock.json
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy,rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: backend-rs
      - name: install gh (Linux)
        if: runner.os == 'Linux'
        run: |
          type -p gh >/dev/null || (
            curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg \
              | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg &&
            echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" \
              | sudo tee /etc/apt/sources.list.d/github-cli.list >/dev/null &&
            sudo apt-get update && sudo apt-get install -y gh
          )
      - name: install gh (macOS)
        if: runner.os == 'macOS'
        run: brew install gh
      - name: cargo fmt
        run: cargo fmt --all -- --check
        working-directory: backend-rs
      - name: cargo clippy
        run: cargo clippy --all-targets -- -D warnings
        working-directory: backend-rs
      - name: cargo test
        run: cargo test --release -- --test-threads=1
        working-directory: backend-rs
      - name: cargo build bins
        run: cargo build --release --bins
        working-directory: backend-rs
      - name: web test
        run: npm ci && npm run test
        working-directory: web
      - name: web build
        run: npm run build
        working-directory: web
      - name: production web bundle gate
        run: |
          test -f codoxear/static/dist/index.html
          ! grep -R "./src/main.tsx" codoxear/static/dist/index.html
      - name: source file line limit
        run: |
          oversized="$(find backend-rs/src -name '*.rs' -print0 | xargs -0 wc -l | awk '$2 != "total" && $1 > 800 {print}')"
          if [ -n "$oversized" ]; then
            echo "$oversized"
            exit 1
          fi
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: install contract test runner
        run: python -m pip install pytest
      - name: rust-only contract tests
        run: pytest tests/contract -q
